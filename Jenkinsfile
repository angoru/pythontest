pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile'
        }
    }
    stages {
        stage('Install') {
            steps {
                sh 'ls -lah'
            }
        }
    }
}
